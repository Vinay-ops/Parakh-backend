# ML / OCR pipeline dependencies (Python 3.10 required — PaddlePaddle has no
# wheels for Python 3.11+ on Linux as of 2026).
#
# Install separately AFTER requirements.txt:
#   pip install -r requirements-ml.txt
#
# On a CUDA-capable host, replace paddlepaddle with paddlepaddle-gpu.
paddlepaddle>=2.6.0
paddleocr>=2.9.1
opencv-python-headless>=4.9.0.80
